items_lst = input().split(", ")
dictionary = {key: ord(key) for key in items_lst}
print(dictionary)

#for element in items_lst:
    #key = element
    #value = ord(element)
    #dictionary[key] = value
#print(dictionary)


